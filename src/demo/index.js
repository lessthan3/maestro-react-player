import React from 'react'
import { render } from 'react-dom'
import App from './App'
import Maestro from './MaestroPlayer'
const container = document.getElementById('app')

//render(<App />, container)
render(<Maestro />, container)
